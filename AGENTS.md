# DOT Repo Notes

- Canonical dotfiles repo path is `~/dot`.
- `dot` is also aliased in shell workflows to `~/.dot`.
- Neovim config source of truth in this repo is `config/nvim/`.
- After editing files in this repo, run `rcup` to sync changes into `~/`.
