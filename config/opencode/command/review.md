Record or read review gate status for a plan phase.

Rules:
- During active live execution, run with `--set pass|fail`.
- Do not use `review` as a status-only poll in an active loop.
- Use `review-runs` to inspect history when needed.
