---
description: Generate hierarchical AGENTS.md knowledge base with deep analysis
---

Generate hierarchical AGENTS.md files for the current codebase using the init-deep workflow.

First, invoke the skill tool to load the init-deep skill:

```
skill({ name: 'init-deep' })
```

Then follow the skill instructions to analyze and generate documentation.

<user-request>
$ARGUMENTS
</user-request>
