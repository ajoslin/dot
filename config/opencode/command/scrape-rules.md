---
description: Extract quality rules from PR review feedback and codify into enforcement layers
---

Load the `scrape-rules` skill. Run the full pipeline: scrape PR comments, analyze in parallel batches, synthesize, classify into enforcement layers, and implement.

Input: $ARGUMENTS
