# Session Learnings

- User wants plugin-free adoption of Oh My OpenCode ideas (no `oh-my-opencode` plugin).
- Keep `build` as primary orchestrator name.
- Add `build-junior` as focused execution subagent.
- Remove custom `claude` agent file.
- Prioritize GPT-5.4-only setup (no mixed model policy unless explicitly requested).
- Prefer minimal, smooth integration first, then deepen in phased passes.
- `/ulw-loop` is not implemented in this local config repo; investigate externally/docs/source before advising.
- User prefers ULW gate syntax: `/ulw-loop "task" --gate "..." --gate "..."`, with Oracle as default verifier unless `@agent` override is provided.
- User wants planning-first recommendations for minimal, high-value OHMO features to port into local plugin-free setup.
